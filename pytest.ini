[pytest]
addopts = -p no:socket -p no:homeassistant-custom-component
markers =
    asyncio: mark a test as asyncio-based
    online: tests that perform real HTTP requests (opt-in with RUN_ONLINE=1)


